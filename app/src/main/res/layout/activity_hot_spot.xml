<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".HotSpotActivity">

    <TextView
        android:id="@+id/beacon_identifier_label"
        android:text="@string/beacon_identifier_label"
        android:labelFor="@+id/beacon_identifier"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <TextView
        android:gravity="center_vertical"
        android:layout_below="@+id/beacon_identifier_label"
        android:layout_alignBottom="@+id/beacon_identifier"
        android:id="@+id/beacon_identifier_preamble"
        android:text="@string/beacon_identifier_preamble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <EditText
        android:layout_toRightOf="@+id/beacon_identifier_preamble"
        android:layout_below="@+id/beacon_identifier_label"
        android:id="@+id/beacon_identifier"
        android:text="@string/default_beacon_identifier"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:layout_below="@+id/beacon_identifier"
        tools:text="AdvertiseButton"
        android:text="@string/advertise"
        android:id="@+id/advertise_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:layout_below="@+id/advertise_button"
        tools:text="ScanButton"
        android:text="@string/scan"
        android:id="@+id/scan_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>
